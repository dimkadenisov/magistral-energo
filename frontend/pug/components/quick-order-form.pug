mixin quick-order-form(props = {})
  include input
  include button
  include checkbox

  - const { callback = false } = props;
  - const buttonText = callback ? 'Перезвонить' : 'Получить счет';

  form.quick-order-form&attributes(attributes)
    if !callback
      +input({labelText: 'Введите Название компании или ИНН', required: true,})(required="required")
      +input({labelText: 'E-mail', required: true,})(required="required" type="text" autocomplete="off" autocorrect="off" spellcheck="false" autocapitalize="off" maxlength="250")

    +input({labelText: 'Телефон', required: true,})(type="tel" inputmode='tel' autocorrect='off' autocomplete='tel')
    +button({variant: 'primary'})(type="submit")= buttonText
    +checkbox({checked: true})
      span.text-small
        |Нажимая кнопку «Получить счет», я даю свое согласие на обработку моих персональных данных, в соответствии с Федеральным законом от 27.07.2006 года №152-ФЗ «О персональных данных», на условиях и для целей, определенных в Согласии на обработку персональных данных
