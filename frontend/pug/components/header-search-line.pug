mixin header-search-line

  .header-search-line
    .container
      .row
        .col-lg-auto.d-none.d-lg-block
          .d-flex
            +logo({classes: 'd-none'})
            +button({variant: 'primary', type: 'icon_left', iconId: 'burger'})
              |Каталог товаров
        .col-12.col-lg-auto.flex-grow-1
          .d-flex
            +header-search
